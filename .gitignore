# .gitignore for a TypeScript-based NodeJS module
#
# This does not rely on a given template, but will be completed "as we go".

# don't track NodeJS module directory
node_modules

# ignore the package-lock.json while setting up the module layout
# TODO: Make sure to remove this line before merging back to "development"
package-lock.json
